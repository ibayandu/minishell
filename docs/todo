 - Add wildcard flag for word
 - Add builtin flag for builtin command words
 - Add token for $?
 - Configure read_a_line function to add "\n" at the end of input.
 Also use it every place that u need to use readline function.
 Until then, u should use "\n" at the end of your inputs.
 - Handle incomplete command on repl loop
 - Handle Ctrl + D action on repl loop. U should exit on direct Ctrl + D and ignore on any prompt.
 - Decide similar approach on directory structor for total structs and definations
 - Close the 3 to MAX_FD fd's on new process with fork
 - Add Assignment Builtin flag for export
 - The builtin redirections are missing
 - builtin pipes won't working
